{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@8.19.2",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.2"
  }
}
